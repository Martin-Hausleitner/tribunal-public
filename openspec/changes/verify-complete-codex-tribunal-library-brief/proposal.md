## Why

The repository already contains a completed Tribunal implementation and earlier live evidence, but the requested delivery depends on mutable external state: NotebookLM authentication and corpus behavior, OSS metadata, independent judge availability, package installation, and the public remote. A fresh fail-closed verification is required before claiming that the brief remains complete.

## What Changes

- Reverify the canonical NotebookLM notebook with authenticated source inventory, role-aligned cross-queries, and a contradiction/source-attribution control.
- Refresh primary OSS metadata and re-evaluate the published 100-point matrix, numeric stars, license qualifications, and unique crown.
- Obtain three fresh, mutually isolated `agy` judgments for knowledge/correctness, harsh criticism/risks, and CLI UX/implementability; retain provider provenance and exclude incomplete or unsupported outputs.
- Update the public report and evidence ledgers so the IDR declaration, three verdicts, synthesis, Mermaid flow, limitations, and implementation plan describe this exact verification pass.
- Exercise source-tree, build, clean-install console, and installed Python API surfaces with a real comparison case and explicit failure-path checks.
- Strictly gate, commit, push, and retrieve the exact SHA-pinned public report blob.

## Capabilities

### New Capabilities

- `brief-completion-verification`: Defines fresh evidence, isolation, executable proof, and immutable publication requirements for verifying the completed Codex Tribunal Library brief.

### Modified Capabilities

None.

## Impact

The change affects OpenSpec artifacts, `report/codex-trib-lib-tribunal.md`, structured comparison data if live facts changed, and new evidence ledgers under `report/evidence/`. Runtime or tests change only if current E2E proof exposes a scoped defect. External systems in scope are authenticated NotebookLM, primary GitHub metadata, the brief-authorized `agy` judge fallback, Python packaging, Git, and the public GitHub blob surface.
