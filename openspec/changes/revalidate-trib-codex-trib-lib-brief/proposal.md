## Why

The public Codex Tribunal delivery is already implemented, but its decisive evidence depends on mutable external state: NotebookLM authentication and grounding, model-provider availability, GitHub metadata, package installation, and the pushed public blob. A fresh, OpenSpec-bound acceptance pass is required so the supplied live brief is proven in this session rather than inferred from earlier successful audits.

## What Changes

- Revalidate the canonical NotebookLM notebook with authenticated source inventory, three role-aligned cross-queries, and one contradiction/source-attribution control.
- Attempt three isolated Grok 4.5 high-effort judgments for knowledge/correctness, hostile criticism/risk, and UI/UX feasibility, using only the brief-authorized `agy` fallback when a Grok call returns no complete verdict.
- Refresh primary GitHub metadata for the OSS-first candidate set and recheck the differentiated 100-point matrix with exactly one evidence-eligible crown.
- Run the real source-tree and clean-install Tribunal CLI/API paths, including invalid-input behavior, without mocked backend or model output.
- Publish a dated revalidation ledger and update the public report only where current evidence changes or qualifies a claim.
- Pass all deterministic gates, commit the intended artifacts, push the branch, and verify the exact SHA-pinned public report blob.

## Capabilities

### New Capabilities

- `trib-brief-live-revalidation`: Defines this session's fail-closed contract for current IDR, three independent perspectives, OSS comparison, executable proof, and immutable public handoff.

### Modified Capabilities

None.

## Impact

The change affects OpenSpec planning artifacts, a new sanitized evidence ledger under `report/evidence/`, the structured GitHub snapshot/matrix and public tribunal report if mutable facts changed, and runtime/tests only if the current E2E exposes a scoped defect. External systems in scope are authenticated NotebookLM, GitHub, Grok or the explicitly permitted `agy` fallback, Python packaging, Git, and the public GitHub blob surface.
