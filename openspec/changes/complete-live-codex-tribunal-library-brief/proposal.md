## Why

The requested Codex Tribunal Library delivery depends on mutable live evidence: an authenticated NotebookLM corpus, current OSS metadata, fresh independent judge calls, executable package behavior, and a pushed public report. Earlier completed audits are useful history but cannot prove that the exact brief still succeeds in this session.

## What Changes

- Run a new authenticated NotebookLM IDR against the canonical notebook, including source checks, three role-aligned cross-queries, and a contradiction control.
- Recheck the OSS-first candidate set against primary GitHub metadata and enforce a differentiated 100-point matrix with exactly one evidence-eligible crown.
- Attempt three isolated Grok 4.5 high-effort judgments for knowledge/correctness, harsh criticism/risk, and UI/UX feasibility, retaining truthful failure and authorized fallback provenance.
- Refresh the public tribunal report and durable evidence so its IDR declaration, three verdicts, synthesis, Mermaid flow, limitations, and implementation plan agree.
- Exercise the recommendation through source-tree and clean-install CLI/API paths with no mocked backend or model results.
- Gate, commit, push, and verify the exact SHA-pinned public report blob.

## Capabilities

### New Capabilities

- `brief-live-closure`: Defines the fail-closed contract for this brief's fresh IDR, three-role tribunal, OSS synthesis, real E2E proof, and immutable public handoff.

### Modified Capabilities

None.

## Impact

The change affects OpenSpec planning artifacts, `report/codex-trib-lib-tribunal.md`, structured matrix/snapshot data if live facts changed, evidence ledgers under `report/evidence/`, and runtime or test files only if current E2E exposes a scoped defect. External systems in scope are authenticated NotebookLM, GitHub, Grok or the explicitly allowed `agy` fallback, Python packaging, Git, and the public GitHub blob surface.
