# Brief live verdict: hostile criticism and risk

Engine: authorized `agy` fallback, Claude Sonnet 4.6 (Thinking)  
Session: fresh project restricted to `/home/coder/tribunal-public`, plan/read-only sandbox  
Score: `73/100`  
Recommendation: **Ship with conditions**

## 1. Assigned perspective and inspected scope

Hostile attempt to falsify the bounded local ship claim. The accepted retry listed only packet-permitted runtime, README, package, skill, persona, tests, examples, gate, matrix, baseline, IDR, OSS, and GitHub snapshot surfaces. The prior cross-repository session is excluded.

## 2. Severity-ordered findings

1. **High:** The external `85/100` comparison crown is a human/evidence rubric, not a live semantic runtime result. Arithmetic gates cannot independently prove its semantic provenance.
2. **High at freeze:** The packet had compile/import/wheel-listing controls but not yet a fresh clean-install console/API run. Publication must wait for that E2E.
3. **Medium:** A custom backend self-declares its score and empty gap list. The marker gate cannot independently verify that the backend investigated all gaps; this is documented but material for live integrations.
4. **Medium:** Capacity is deliberately per-run and stateless; operators must not treat `quota_file` as durable provider/billing enforcement.
5. **Low/medium:** Markdown intentionally flattens backend newlines while JSON remains lossless; operators relying on Markdown should understand that representation trade-off.
6. **Low/medium:** The CSV gate validates enum cells and arithmetic, not substantive evidence behind each capability marker. Report prose must keep matrix fit separate from runtime/visual proof.
7. **Low:** Persona repetition after round three is disclosed but has no additional runtime warning.

The judge explicitly refuted the NotebookLM-invented syntax, missing backend, missing validator, missing persona package, raw-target, and Pydantic claims.

## 3. Component scores

| Component | Score |
|---|---:|
| Type Fit | 20/25 |
| Adversarial Depth | 13/20 |
| Evidence Provenance | 14/20 |
| Persona/Skill Extensibility | 13/15 |
| Observability/Repeatability | 6/10 |
| Integration Cost | 7/10 |
| **Checked total** | **73/100** |

## 4. Evidence inspected

The judge read the full runtime, README, packaging, skill, representative synthetic persona, tests, core example, CSV gate/matrix, baseline, live IDR, OSS ledger, and primary GitHub snapshot. It verified the central symbols and documented non-goals directly.

## 5. Evidence gaps and unsupported claims

- No clean-install console/API run was available in the frozen packet; this must be closed after judging.
- Matrix rubric points are an external assessment rather than `TribunalOrchestrator.judge()` output.
- The `UI/UX` capability cell cannot be read as visual proof; no browser/viewport evidence exists.
- No bundled backend queries NotebookLM or supplies semantic evidence.

## 6. Recommendation and actions

**Ship with conditions.** Complete and retain the fresh exact-wheel console/API E2E; state plainly that matrix scores are external comparative fit rather than local runtime quality; avoid reading a UI/UX capability marker as visual validation; preserve the self-declared-backend trust boundary and stateless-capacity limit.

## 7. Attestation

I did not inspect any other brief-live judge output.
