# NotebookLM revalidation evidence

Recorded UTC: `2026-07-20T19:19:06Z`

## Canonical notebook and corpus

- Notebook ID: `80cffd38-0185-4f4d-ae00-bbc67c4bc515`
- Title: `Tribunal IDR 2026-07-04`
- Public link: https://notebooklm.google.com/notebook/80cffd38-0185-4f4d-ae00-bbc67c4bc515
- Authenticated CLI: `nlm 0.8.9`
- Total sources: `336`
- Processed sources: `331`
- Failed sources: `5`

The authenticated CLI independently resolved the notebook by exact ID, confirmed public-link sharing, and listed all sources. The five failed entries were duplicated or secondary Medium articles about promptfoo, LLM judges, and AutoGen. Current project artifacts and the targeted primary OSS repository sources were processed. Private account and collaborator data are omitted.

## New cross-queries

All four calls used notebook conversation ID `1ebabb83-484f-405b-8b24-a26cfa5b9afb`. This identifies the notebook conversation; it is not evidence of statistical independence. The first three role-aligned calls were initiated separately. The fourth was deliberately executed afterward as a contradiction and source-attribution control.

### Query 1: knowledge and correctness

**Question:** Revalidate the current Codex Tribunal release using only processed project artifacts and authoritative primary sources. Separate executable structural guarantees from semantic correctness, visual quality, provider-family independence, and production observability. Return source-attributed supported claims, unsupported claims, unresolved evidence gaps, and the minimum honest recommendation.

**Returned metadata:** `15` source IDs and `46` citation mappings.

**Supported result:** The package has executable structural validation, stable serialization, a bounded local score, explicit evidence gaps, and honest post-hoc synthesis. It is not a semantic fact checker, visual tester, family-diversity enforcer, or persistent production trace system.

**Manual corrections:** The answer used the stale pre-audit count of `330`, imported an unrelated 70-row model registry, called the per-run capacity planner stateful enforcement, and attached study-specific correlation values to a hypothetical same-family deployment as though they were measured properties of this project. Those claims are excluded.

### Query 2: hostile risks

**Question:** Act as a hostile risk auditor of the current Codex Tribunal release. Use processed project artifacts and primary sources. Rank reproducible defects and future live-backend risks by severity, distinguish implemented behavior from explicit non-goals, and give the smallest defensible mitigations with source attribution and confidence.

**Returned metadata:** `15` source IDs and `33` citation mappings.

**Supported result:** A future live backend must address provider/model provenance, correlated judges, style and position bias, executable proof, durable budgets, and state recovery. These are integration risks, not capabilities secretly claimed by the dependency-free core.

**Manual corrections:** The answer referred to Pydantic even though the runtime is standard-library-only, promoted a study-specific error-correlation range to a universal local defect, and prescribed model-family enforcement inside the core despite the documented thin-core boundary. Those overextensions are not adopted.

### Query 3: CLI UX and feasibility

**Question:** Audit the current Codex Tribunal CLI, Python API, reusable skill, persona library, packaging, and evidence workflow for usability and implementation feasibility. Use processed project artifacts and primary sources. Separate observable CLI behavior from claims requiring a GUI or human study, identify friction and adoption risks, and give a bounded recommendation with evidence gaps.

**Returned metadata:** `0` source IDs and `0` citation mappings.

**Usable result:** None of this answer counts as grounded research evidence. Its high-level CLI observations are hypotheses to verify by execution, not source-backed findings.

**Manual corrections:** The fluent answer cited source names in prose while returning empty grounding arrays, repeated study-specific correlation and market-churn numbers, and discussed cryptographic controls that are explicit non-capabilities. This is a concrete anti-fake failure and is preserved rather than silently upgraded to evidence.

### Query 4: contradiction and attribution control

**Question:** Audit the three preceding answers for attribution and contradictions, explicitly testing the live `336/331/5` source counts, the unrelated 70-row registry, stateless capacity planning, the limits of separate calls/personas, study-specific metrics, and the empty UX grounding arrays.

**Returned metadata:** `8` source IDs and `24` citation mappings.

**Grounded result:** The control confirmed all named mismatches: `330` was stale; the 70-row registry belongs to another research artifact; local capacities are planning inputs rather than durable quotas; separate calls are not provider memory or family independence; the numerical correlations and churn rates are not local measurements; and the UX answer was ungrounded at the returned-metadata level.

**Residual correction:** The control still described general literature as if its recommended controls were mandatory implementation details. The release adopts only the conservative boundary: ship the library as a local structural orchestration contract and reusable skill, and require separate proof for semantic, visual, provider, budget, and durability claims.

## Revalidation conclusion

The new calls do not broaden the green claim. They reinforce the prior verdict and demonstrate the need for manual contradiction checks: NotebookLM is valuable for cross-source discovery, but its prose can mix stale project snapshots, unrelated papers, secondary metrics, and explicit non-goals. Current executable behavior and primary project evidence outrank generated characterization.
